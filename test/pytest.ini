[pytest]

testpaths = test/testcase/


; python_functions = test_bulk_create_collection
markers =
     sync: sync test
     a_sync: async test
     bulk: bulk test

log_cli = False

; addopts = --clean-alluredir  --alluredir ./report
;  --reruns 3 -s  -n auto  --dist-mode=each  --dist=loadfile
